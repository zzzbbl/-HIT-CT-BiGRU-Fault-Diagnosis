# Notebooks

This directory is reserved for optional lightweight demonstration notebooks.
Before committing a notebook, clear large outputs and ensure that it does not
embed datasets, checkpoints, or generated figures.
