seed: 42
device: auto

data:
  data_dir: data/hit_full
  class_names:
    - Healthy
    - Inner Fault
    - Outer Fault

model:
  name: ct_bigru
  input_channels: 4
  num_classes: 3

training:
  batch_size: 16

noise:
  snr_levels: [-5, 0, 5, 10, 15, 20]

